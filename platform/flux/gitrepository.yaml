apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: atlas-talos
  namespace: flux-system
spec:
  interval: 1m
  url: ssh://git@github.com/austin2153/atlas-talos.git
  ref:
    branch: main
  secretRef:
    name: flux-system-auth
