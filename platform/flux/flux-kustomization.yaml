apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: kratix-state
  namespace: flux-system
spec:
  interval: 1m
  sourceRef:
    kind: GitRepository
    name: atlas-talos
  path: ./state
  prune: true
